# Image Prediction API - Python Dependencies
# IMPORTANT: Install PyTorch FIRST (required):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Then install the rest:
#   pip install -r requirements.txt
#
# For Render deployment, use setup.sh which handles the order automatically.

# Backend
fastapi
uvicorn
pillow
python-multipart
numpy

# Frontend (optional — for Streamlit UI)
streamlit
requests
